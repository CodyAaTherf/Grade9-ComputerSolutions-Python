# Write a program to find the population density of a region

population = int(input("Enter the population: "))
area = float(input("Enter the area: "))
density = population / area

print(f"{population} / {area} = {density}")
