# Write a program to calculate the area of a rectangle with arbitrary values of length and breadth

l = float(input("Enter length: "))
b = float(input("Enter breadth: "))
a = l * b
print(f"Area of rectangle = {a}")